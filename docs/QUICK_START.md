# ⚡ Быстрый старт AI Scientometer

> Запустите AI Scientometer Assistant за 5 минут!

## 🚀 Экспресс установка

### 1️⃣ **Клонирование и установка**
```bash
# Клонируем репозиторий
git clone https://github.com/yourusername/ai-scientometer.git
cd ai-scientometer

# Устанавливаем Python зависимости
pip install -r requirements.txt

# Устанавливаем Frontend зависимости
cd ai-dashboard && npm install && cd ..
```

### 2️⃣ **Настройка базы данных**
```bash
# Создайте файл .env с вашими данными MongoDB
cp env_example.txt .env
# Отредактируйте .env файл с вашими данными
```

### 3️⃣ **Запуск системы**
```bash
# Терминал 1: Backend
python3 scientometer.py

# Терминал 2: Frontend
cd ai-dashboard && npm start
```

### 4️⃣ **Готово! 🎉**
- **Frontend**: http://localhost:3002
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs

## 💬 Первые шаги в чате

Откройте AI Chat и попробуйте:

```
👋 "Привет!"
🔍 "Найди статьи про машинное обучение"
📰 "Посоветуй журнал для AI"
🎓 "Объясни что такое нейронные сети"
💻 "Покажи код на Python"
```

## 🔧 Быстрые команды

```bash
# Проверка статуса
curl http://localhost:8000/api/v1/stats

# Тест чата
curl -X POST http://localhost:8000/api/v1/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "Привет!"}'

# Остановка всех процессов
pm2 stop all  # если используете PM2
# или Ctrl+C в терминалах
```

## 🆘 Проблемы?

- **Порт занят**: `lsof -i :8000` → `kill -9 PID`
- **MongoDB ошибка**: Проверьте строку подключения в `.env`
- **npm ошибки**: `rm -rf node_modules && npm install`

**Нужна помощь?** Читайте полный [README.md](README.md) 📖
